version: '3.8'
services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: report-rabbitmq
    ports:
      - "5673:5672"
      - "15673:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - report-network
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 10s
      timeout: 5s
      retries: 3

  phonedirectory-api:
    build:
      context: .
      dockerfile: PhoneDirectory.API/Dockerfile
    container_name: phonedirectory-api
    ports:
      - "5001:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
      - RabbitMQ__HostName=rabbitmq
      - RabbitMQ__Username=guest
      - RabbitMQ__Password=guest
      - ConnectionStrings__DefaultConnection=Server=DESKTOP-KUS4Q12\\SQLEXPRESS;Database=PhoneDirectory;Trusted_Connection=True;Encrypt=True;TrustServerCertificate=True;
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - report-network
    depends_on:
      rabbitmq:
        condition: service_healthy

  report-api:
    build:
      context: .
      dockerfile: Report.API/Dockerfile
    container_name: report-api
    ports:
      - "5003:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
      - RabbitMQ__HostName=rabbitmq
      - RabbitMQ__Username=guest
      - RabbitMQ__Password=guest
      - ConnectionStrings__DefaultConnection=Server=DESKTOP-KUS4Q12\\SQLEXPRESS;Database=Report;Trusted_Connection=True;Encrypt=True;TrustServerCertificate=True;
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - report-network
    depends_on:
      rabbitmq:
        condition: service_healthy

volumes:
  rabbitmq_data:

networks:
  report-network:
    driver: bridge